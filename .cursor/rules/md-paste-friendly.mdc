---
description: 回覆中的對照表／規格摘要須可直接貼進 .md 文件
alwaysApply: true
---

# 可貼進 Markdown 文件的回覆格式

使用者常把對話裡的對照、規格、操作說明貼進自己的 `.md`。聊天室渲染後的表格複製出去通常不是 Markdown 原始碼。

## 要記下的對照或規格

用 **Markdown 程式碼區塊** 包住完整原始碼（含管線表格），方便整段複製：

````
```markdown
| 鍵入 | 輸入框 | 候選 |
|------|--------|------|
| `kiunn` | `kiuⁿ` | `kiunn1` |
```
````

- 表格用 GitHub 管線語法（`|` + 分隔列）。
- 區塊語言標 `markdown`，不要只丟渲染表格。
- 短對照也可用項目清單（複製即為 `.md`），但超過兩欄時仍用上述表格區塊。

## 不要

- 不要只輸出會被 UI 畫成格子的裸表格，卻不附可複製的原始碼。
- 不要用 HTML `<table>`。
