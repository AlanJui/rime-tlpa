# 工作日誌

## 專案發行指引文件

● 專案的打包與輸入方案相關檔案主要在這兩個地方：

  ### 打包相關

  | 檔案 | 說明 |
  |------|------|
  | `build/build_installer.py` | 打包安裝程式的主腳本 |
  | `build/build_system.py` | 建置系統 |
  | `build/rime_installer.py` | RIME 安裝器 |
  | `build/installer_gui.py` | 安裝器 GUI |
  | `RIME_TLPA_Installer.spec` | PyInstaller 打包規格 |
  | `RIME_TLPA_Installer_New.spec` | 新版打包規格 |
  | `build/PACKAGING_GUIDE.md` | 打包說明文件 |

  ### 部署輸入方案

  | 檔案 | 說明 |
  |------|------|
  | `deploy_rime_tlpa.ps1` | PowerShell 部署腳本（主要） |
  | `deploy_rime.bat` | Batch 部署腳本 |
  | `deploy_simple.bat` | 簡易部署 |
  | `redeploy_rime.ps1` | 重新部署 |

  ### 輸入方案本體

  | 檔案 | 說明 |
  |------|------|
  | `phing_im_tlpa.schema.yaml` | 拼音 TLPA 輸入方案 |
  | `huan_ciat_tlpa.schema.yaml` | 換切 TLPA 輸入方案 |
  | `zu_im_tlpa.schema.yaml` | 注音 TLPA 輸入方案 |
  | `huan_ciat_tps.schema.yaml` | 換切 TPS 輸入方案 |

## 建置 Python 作業環境

### 建置 Python 虛擬環境

在【專案根目錄】，建置 Python 虛擬環境 `.venv` 。

```bash
cd C:\Users\AlanJui\work\rime-tlpa
py -m venv .venv
```

---

### 需要安裝的 Python 套件

（從各 .py 檔的 import 掃描而來）
  | 套件名稱 | 用途 | 備註 |
  |----------|------|------|
  | `pandas` | 資料處理 | 需安裝 |
  | `openpyxl` | 讀寫 Excel `.xlsx` | 需安裝 |
  | `xlwings` | Excel 操作（需有 Excel） | 需安裝 |
  | `pypandoc` | 文件格式轉換 | 需安裝（且需安裝 Pandoc） |
  | `tkinter` | GUI 介面 | Python 內建，無需安裝 |
  | `pyinstaller` | 打包 `.exe` | 需安裝（見 `.spec` 檔） |

```powershell
 PS C:\Users\AlanJui\work\rime-tlpa> .\.venv\Scripts\Activate.ps1
(.venv) PS C:\Users\AlanJui\work\rime-tlpa> pip install -r .\requirements.txt
```

  注意：pypandoc 除了 pip 套件外，還需另外安裝 https://pandoc.org/installing.html
  本體才能正常運作。

  winget install --source winget --exact --id JohnMacFarlane.Pandoc

● 如果專案中有用到 pypandoc，就需要安裝 Pandoc 本體。

● 只有 src/make_install_sop.py
  一支程式用到。看檔名是製作安裝說明文件用的，屬於輔助工具，不是核心功能。

  結論：

  - 若不需要執行 make_install_sop.py，可以暫時跳過 winget install Pandoc
  - 若日後需要產生安裝說明文件，再補裝即可

---

## 待完成輸入方案

### 輸入方案

本專案所開發之【輸入方案】，其所支援之【漢字標音】標準，大致可分以下三大類別：

### 輸入方案類別

目前已開發之輸入方案，共分下述三大類：

- 拼音輸入法：使用羅馬拼音字母的輸入法
- 注音輸入法：類似中文的注音符號輸入法
- 反切輸入法：採用《彙集雅俗通十五音》的反切輸入法

上述三種類輸入法，其所使用的閩南話漢字標音系統，有如下之關聯：

- 拼音輸入法：使用【羅馬拼音字母】當作【音標】之輸入法
  - 台語音標 (TLPA+)
  - 台羅拼音 (TL)
  - 白話字 (POJ)
  - 閩拼方案 (BP)
  - 台語注音二式 (BPM2)
- 注音輸入法：使用【注音符號】改良後適用於閩南話之【注音】輸入法
  - 十五音 (SNI)：此指《彙集雅俗通十五音》
- 方音符號 (TFS)
- 國際音標 (IPA)

### 1. 拼音輸入法

使用「羅馬拼音字母」輸入法，支援的輸入方案有：

- 台語音標（TLPA+）：phing_im_tlpa
- 台羅拼音（TL）：phing_im_tl
- 白話字（POJ）：phing_im_poj
- 台語注音二式（BPM2）：phing_im_bpm2
- 閩拼方案（BP）：phing_im_bp

### 2. 注音輸入法

使用依據閩南話發音特性改良的「注音符號」輸入法（目前：僅支援吳守禮先生的【方音符號】）。

但輸入方案使用之「字典」，其「注音編碼」分兩種標準：

- 台語音標（TLPA+）：zu_im_tlpa
- 台語注音二式（BPM2）：zu_im_bpm2

### 3. 反切輸入法

目前尚非「實體」的十五音反切輸入法。反切使用的十五音：【聲母】、【韻母】，仍借用【注音符號】、【羅馬拼音字母】按鍵輸入；經輸入方案轉換成【雅俗通十五音】使用之【聲母】、【韻母】及【調號】。

- 反切輸入法【方音符號】：huan_ciat_tps
- 反切輸入法【台語音標】：huan_ciat_tlpa

#### 輸入方案對照表

| 輸入法類別 | 輸入方案名稱               | 輸入方案識別號 | 字典編碼     | 漢字標音系統全寫   |
| --------- | -------------------------- | -------------- | ------------ | ------------------ |
| 拼音輸入法 | 拼音輸入法【台語音標】     | phing_im_tlpa  | 台羅拼音     | 台語音標(TLPA+)    |
| 拼音輸入法 | 拼音輸入法【台羅拼音】     | phing_im_tl    | 台羅拼音     | 台羅拼音(TL)       |
| 拼音輸入法 | 拼音輸入法【白話字】       | phing_im_poj   | 台羅拼音     | 白話字(POJ)        |
| 拼音輸入法 | 拼音輸入法【閩拼方案】     | phing_im_bp    | 台羅拼音     | 閩拼方案(BP)       |
| 拼音輸入法 | 拼音輸入法【台語注音二式】 | phing_im_bpm2  | 台語注音二式 | 台語注音二式(BPM2) |
| 注音輸入法 | 注音輸入法【台語音標】     | zu_im_tlpa     | 台羅拼音     | 台語音標(TLPA+)    |
| 注音輸入法 | 注音輸入法【台語注音二式】 | zu_im_bpm2     | 台語注音二式 | 台語注音二式(BPM2) |
| 反切輸入法 | 反切輸入法【方音符號】     | huan_ciat_tps  | 台羅拼音     | 方音符號(TFS)      |
| 反切輸入法 | 反切輸入法【台語音標】     | huan_ciat_tlpa | 台羅拼音     | 台語音標(TLPA+)    |



### release-include.txt

```powershell
# --- 鍵盤按鍵練習工具 ---
kb_hong_im.schema.yaml
kb_ipa.schema.yaml

# --- 共用函式庫/工具 ---
rime.lua
keymap_piau_tian.yaml
lib_hau_suan_ji_tuann.yaml
lib_phing_im.yaml
lib_sip_ngoo_im.yaml
lib_sip_ngoo_im2.yaml
lib_zu_im.yaml

# --- 支援【台語拼音（TLPA）】 ---
tlpa_phing_im.schema.yaml
tlpa_hong_im.schema.yaml
tlpa_lib_hau_suan_ji_tuann.yaml
tl_ji_khoo_ciann_ji.dict.yaml
tl_ji_khoo_kah_kut_bun.dict.yaml
tl_ji_khoo_kong_un.dict.yaml
tl_ji_khoo_nga_siok_thong.dict.yaml
tl_ji_khoo_peh_ue.dict.yaml
tl_ji_khoo_peh_ue_zu_ting.dict.yaml

# --- 支援【台語注音二式】 ---
zu_im_2_phing_im.schema.yaml
zu_im_2_hong_im.schema.yaml
zu_im_2_libs.yaml
zu_im_2_hau_suan_zzi_duann.yaml
zu_im_2.dict.yaml

# --- 支援【閩拼方案】 ---
bp_hong_im.schema.yaml
bp_phing_im.schema.yaml
bp_libs.yaml
bp_ji_khoo.dict.yaml

```
